import { PipeEstadoPipe } from './pipe-estado.pipe';

describe('PipeEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
